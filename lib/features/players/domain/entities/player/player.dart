import 'package:flutter/material.dart';

class PlayerEntity {
  final int? id;
  final String? name;
  final int? score;
  final Color? color;

  PlayerEntity({
    this.id,
    this.name,
    this.score,
    this.color,
  });
}
